<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Auteurs</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f9f9f9;
        }
        h1 {
            text-align: center;
            color: #4CAF50; /* Couleur verte pour le titre */
            font-size: 2.5em;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 15px;
            text-align: left;
        }
        th {
            background-color: #4CAF50; /* Couleur verte pour les en-têtes */
            color: white;
            font-weight: bold;
        }
        td img {
            width: 100px;
            height: auto;
            object-fit: cover;
            border-radius: 5px;
        }
        td a {
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
        }
        td a:hover {
            color: #0056b3;
            text-decoration: underline;
        }
        .summary {
            margin-top: 20px;
            text-align: center; /* Centrer le texte des résumés */
        }
        .summary p {
            font-size: 1.1em;
            color: #333;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        @media (max-width: 768px) {
            table, th, td {
                font-size: 14px;
            }
            td img {
                width: 75px;
            }
        }
        /* Bouton détails avec couleurs */
        td a {
            background-color: #4CAF50;
            color: white;
            padding: 8px 12px;
            border-radius: 5px;
            text-align: center;
            display: inline-block;
            transition: background-color 0.3s;
        }
        td a:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Liste des Livres</h1>

        {% if not books is defined or books is empty %}
            <p>Aucun livre disponible</p>
        {% else %}
            <table>
                <thead>
                    <tr>
                        <th>id</th>
                        <th>title</th>
                        <th>category</th>
                        <th>published</th>
                         <th>Author</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for book in books %}
                        <tr>
                            <td>{{ book.id }}</td>
                            <td>{{book.titre }}</td>
                            <td>{{ book.category }}</td>
                            <td>{{ book.published}}</td>
                            <td>{{ book.author.username}}</td>
                            <td> <form action="{{ path('app_delete_book', {id: book.id}) }}" method="post" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet auteur ?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book.id) }}">
                        <button type="submit" style="color: red; background: none; border: none; cursor: pointer;">Supprimer</button>
                    </form>
                           </td>
                              
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
            
            <div class="summary">
                
                {% set total_published = 0 %}
                {% set total_unpublished = 0 %}
                {% for book in books %}
                 {% if book.published == true %}
                    {% set total_published = total_published + 1 %}
                  {% else %}
                     {% set total_unpublished = total_unpublished + 1 %}
                     {% endif %}
                {% endfor %}
                 <p><strong>unpublished :</strong> {{ total_unpublished}}</p>
                <p><strong>published :</strong> {{ total_published}}</p>

            </div>
        {% endif %}
    </div>

</body>
</html>
